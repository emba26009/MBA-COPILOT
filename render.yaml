services:
  - type: web
    name: mba-copilot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MBA_COPILOT_MODEL
        value: gpt-4.1-mini
